const handleOtpService = require('../helpers/handle-otp')
const handleAuthenticateService = require('../helpers/handle-authenticate')

module.exports = {
  handleOtpService,
  handleAuthenticateService,
}
